import { PageContainer, PageHeader } from '@sovereignfs/ui';

export default function LedgerHomePage() {
  return (
    <PageContainer>
      <PageHeader title="Ledger" description="Your budget, at a glance." />
      <p>Coming soon — the setup wizard and budget dashboard land in upcoming tasks.</p>
    </PageContainer>
  );
}
